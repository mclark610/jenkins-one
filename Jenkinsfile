pipeline {
    agent any 
    

    stages {
        stage('build') {
            steps {
	        sh 'echo "Starting npm version"'
		sh 'npm --version'
		sh 'ls -lrta'
            }
        }
    }
}
